# Sport Scheduler

1. Clone / copy files
2. npm install
3. Copy .env.example to .env and set MONGO_URI (default is local) and SESSION_SECRET
4. npm run seed  # creates admin + sample teams/events
5. npm run dev   # or npm start
6. Open http://localhost:3000
7. Admin login: admin@gmail.com / admin123
